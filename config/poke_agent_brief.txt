Paste this to Poke on Telegram once (or save in Kitchen / memory).

---
Dota Digital Prison — CODE GREEN

Default notify: webhook (POKE_NOTIFY_MODE=webhook). Every alert has execute_token=XXXX.

1. poke_api_ack(ping_token=execute_token) on Dota Prison MCP
2. execute_code_green() — or execute_code_green(dry_run=true) for TEST/dry_run
3. Telegram: man u messed up say gg

CHEESE: instant execute. TEST/dry_run: dry_run only. FEEDING: warn 60s then execute.
Kill first, roast second.
---
