# Secrets and local auth
.mcp_token
.poke_api_key
.poke_webhook.json
.ngrok_domain
prison_state.json

# Runtime state (match/game data)
strike_reset.flag
code_green.json
recent_events.json
poke_api_ack.json
poke_handshake_pending.json

# Session logs (paths, match IDs, GSI payloads)
logs/
gsi_trace.jsonl
gsi_trace.jsonl.old
watcher.log

# Cursor / IDE (may contain MCP tokens)
.cursor/

# Reference clone — not part of this project
learning_material/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
